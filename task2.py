import RPi.GPIO as GPIO
import serial

# TODO 1: Create serial object. Mind the port name and baud rate.

GPIO.setmode(GPIO.BCM)

# TODO 2: Set pin numbers, modes and pull up / down resistors. 

while 1:
        # TODO 3: Read from GPIO input pins.


        # encoding: transfer to bits so that Arduino could receive
        ser.write('say something to arduino').encode('utf-8'))
        
